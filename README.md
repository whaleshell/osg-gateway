# osg-gateway

Apache-2.0 · OpenShell **port** lane (control plane). Binary name: `osg-gateway`.

**Depends on:** `osg-core`, later `osg-port-api` / optionally runtime  
**Must not depend on:** `osg-cli`

```text
osg-gateway/
├── cmd/osg-gateway/
├── internal/gateway/
└── LICENSE / NOTICE
```

See [LICENSING.md](../docs/LICENSING.md).
